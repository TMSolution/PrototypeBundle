<?php 
/**
 * Copyright (c) 2015, TMSolution
 * All rights reserved.
 *
 * For the full copyright and license information, please view
 * the file LICENSE.md that was distributed with this source code.
 */
namespace  {{formTypeNamespace}};

use Symfony\Component\Form\FormBuilderInterface;
use Symfony\Component\OptionsResolver\OptionsResolverInterface;
use Core\BaseBundle\Form\Type\BaseFormType;
use Doctrine\ORM\EntityRepository;

/**
 * FormType  for '{{entityName}}'.
 *
 * Generated with {@see TMSolution\PrototypeBundle\Command\GenerateFormTypeCommand}.
 */
class {{objectName}}Type extends BaseFormType
{

    public function buildForm(FormBuilderInterface $builder, array $options)
    {
        
        $builder
        {% for field, fieldParam in fieldsInfo %}->add('billPeriod', null, [
            'label' => 'Label name',
            'widget' => 'widget name',
            'required' => true,
            'attr' => [
                'placeholder' => 'placeholder text',
                'class' => 'css class name',
            ]
        ]){% if loop.last %};{% endif %}
        
        {% endfor %}
        
    }


    /**
     * @param OptionsResolverInterface $resolver
     */
    public function setDefaultOptions(OptionsResolverInterface $resolver)
    {
        $resolver->setDefaults(array(
            "data_class" => "{{entityName}}",
            'csrf_protection' => false,
        ));
    }

    /**
     * @return string
     */
    public function getName()
    {
        return '{{formTypeName}}';
    }

}